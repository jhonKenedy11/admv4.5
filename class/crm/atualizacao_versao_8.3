################ 13-DEZEMBRO-2024 ATUALIZACAO VERSAO 4.3.2 #######################
Inclusao de coluna na tabela FIN_CLIENTE_CREDITO

ALTER TABLE FIN_CLIENTE 
ADD COLUMN DATANASCIMENTO DATE;

ALTER TABLE FIN_CLIENTE_ACOMP
ADD COLUMN PEDIDO_ID INT(11) DEFAULT NULL AFTER PESSOA;

ALTER TABLE FIN_CLIENTE_ACOMP
ADD COLUMN USERINSERT INT(11) DEFAULT NULL AFTER CUSTOKM;

ALTER TABLE FIN_CLIENTE_ACOMP
ADD COLUMN DATEINSERT TIMESTAMP DEFAULT CURRENT_TIMESTAMP AFTER USERINSERT;

ALTER TABLE FIN_CLIENTE_ACOMP
ADD COLUMN USERCHANGE INT(11) DEFAULT NULL AFTER DATEINSERT;

ALTER TABLE FIN_CLIENTE_ACOMP 
ADD COLUMN DATECHANGE TIMESTAMP NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP AFTER USERCHANGE;


ALTER TABLE FIN_CLIENTE_ACOMP
ADD COLUMN `STATUS` CHAR(1) DEFAULT 'A' AFTER DATECHANGE;


################ 29-JANEIRO-2025 ATUALIZACAO VERSAO 4.0 #######################
Inclusao de tabela FIN_CLIENTE_CREDITO

CREATE TABLE FIN_CLIENTE_CREDITO (
  ID int NOT NULL AUTO_INCREMENT,
  CLIENTE int NOT NULL,
  PEDIDO int NOT NULL,
  NRITEM smallint NOT NULL,
  QUANTIDADE decimal(11,2) DEFAULT '0.00',
  UNITARIO decimal(11,2) DEFAULT '0.00',
  VALOR decimal(11,2) DEFAULT '0.00',
  UTILIZADO decimal(11,2) DEFAULT '0.00',
  EMISSAO date DEFAULT NULL,
  PEDIDOUTILIZADO varchar(20) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci DEFAULT NULL,
  PRIMARY KEY (ID,CLIENTE,PEDIDO,NRITEM)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3;


################ 08/MAIO/2024 ####################
ALTER TABLE FIN_CLIENTE
MODIFY NOMEREDUZIDO VARCHAR(60);

CREATE TABLE `FIN_CLIENTE_OBRA` (
  `ID` int NOT NULL AUTO_INCREMENT,
  `CNO` VARCHAR(20) DEFAULT '0',
  `PROJETO` varchar(90) DEFAULT '0',
  `RESPONSAVEL_TECNICO` int DEFAULT '0',
  `CREA` VARCHAR(20) DEFAULT NULL,
  `ART` VARCHAR(20) DEFAULT NULL,
  `CLIENTE` int NOT NULL,
  `STATUS` char(1) NOT NULL DEFAULT 'A' COMMENT 'A-Ativo, I-Inativo',
  `USERINSERT` int DEFAULT NULL,
  `DATEINSERT` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  `USERCHANGE` int DEFAULT NULL,
  `DATECHANGE` timestamp NULL DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`ID`)
) ENGINE=InnoDB AUTO_INCREMENT=8 DEFAULT CHARSET=utf8mb3;
