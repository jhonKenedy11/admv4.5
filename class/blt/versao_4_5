### 26/09/2025 - Jhon Criacao de log no envio de email

CREATE TABLE `AMB_LOG_EMAIL` (
  `ID` int(11) NOT NULL AUTO_INCREMENT,
  `NUMERO_NOTA` varchar(20) DEFAULT NULL,
  `ID_NOTA` int(11) DEFAULT NULL,
  `USUARIO` varchar(100) DEFAULT NULL,
  `ERRO` text DEFAULT NULL,
  `DATA_HORA` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`ID`)
);

-- ALTERANDO TABELA AMB_USUARIO PARA INCLUIR A TUPULA PORT
ALTER TABLE `AMB_USUARIO`
ADD COLUMN `PORT` INT NULL DEFAULT 587 AFTER `SMTP`;

-- ALTERANDO TABELA AMB_USUARIO PARA INCLUIR A TUPULA EMAILSENHA COM 30 CARACTERES
ALTER TABLE AMB_USUARIO 
MODIFY COLUMN EMAILSENHA VARCHAR(30);